{
  "name": "learningideas",
  "version": "0.1.0",
  "private": true,
  "description": "An interactive guide to ideas across six flagship engineering projects.",
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check data.js && node --check logic.mjs",
    "test": "node --test"
  }
}
